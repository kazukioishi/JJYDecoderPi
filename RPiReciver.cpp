#include <stdio.h>
#include "JJYDecoder.h"

int main(int argc, char* argv[]){
    try{
        JJYDecoder decoder = new JJYDecoder();
        while(0){}
        return 0;
    }catch (char *str) {
        println(str);
        return 1;
    }
}